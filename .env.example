# DOLFINx MCP Server Configuration
# Copy to .env and modify as needed.

# Transport protocol: "stdio" (default) or "sse"
DOLFINX_MCP_TRANSPORT=stdio

# SSE server host (only used when DOLFINX_MCP_TRANSPORT=sse)
DOLFINX_MCP_HOST=127.0.0.1

# SSE server port (only used when DOLFINX_MCP_TRANSPORT=sse)
DOLFINX_MCP_PORT=8000
